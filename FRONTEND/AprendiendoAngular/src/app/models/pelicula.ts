export class Pelicula {
  /* public title: String;
  public image: String;
  public year: number;

  constructor(title: String, image: String, year: number) {
    (this.title = title), (this.image = image), (this.year = year);
  } */

  constructor(
    public title: String,
    public image: String,
    public year: number
  ) {}
}
